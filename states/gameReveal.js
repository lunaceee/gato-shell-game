// this.state.winningShell = Math.floor(Math.random() * 3);
// nextState: Math.random() > 0.5 ? "gatoWon" : "gatoLost"

class GameReveal extends GameState {
  isDone() {}

  onUpdate(dt) {}

  nextState() {}
}
